policy minimal {
    user alice;
    role admin;
    resource doc;
    action read;
    rule allow_read {
        effect: allow
        subject: role admin
        action: read
        resource: doc
    }
}
